# Model assets

Place the supplied files in this directory before running the complete model pipeline:

- `network_anomaly.tflite`
- `accessibility_mlp.tflite`
- `smollm2_360m.gguf`

The app builds without them and reports model unavailability rather than inventing predictions.
